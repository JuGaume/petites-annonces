namespace PetitesAnnonces.Api.Models;

public enum ContactMode
{
    /// <summary>Contact via la messagerie interne (Phase 4).</summary>
    InternalMessaging,

    /// <summary>Coordonnées affichées directement sur l'annonce.</summary>
    DirectContact,
}
